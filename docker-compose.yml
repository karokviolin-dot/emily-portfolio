services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: executive-assistant-landing:latest
    container_name: executive-assistant-landing
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      NODE_ENV: production
